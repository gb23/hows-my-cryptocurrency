<%# <%= list_item_open(params, transaction)  %>
 <li class='list ph3 pv2 b--light-silver'>
    <%= "<div class='mt1 ml6-l ml6-ns'>Coin Type: #{transaction.coin.name}</div>".html_safe if params[:controller] != "wallets"%>
    <div class='ml6-l ml6-ns'>USD spent: <%= format_dollar_amount_of('%.2f' % transaction.money_in, "") %></div>
    <div class='ml6-l ml6-ns'>Price Per Coin: <%= format_dollar_amount_of('%.2f' % transaction.price_per_coin, "") %></div> 
    <div class='ml6-l ml6-ns'>Coins purchased: <%= '%.4f' % transaction.quantity %></div>
    <% if show_link_to_more == true %>
        <%# <%= link_to user_transaction_path(current_user,transaction), class: "link link_left black-80" do %>
            <i data-txid="<%=transaction.id%>" data-image="ellipses" data-userid="<%=current_user.id%>" id="transaction-show-more-icon" class="ml7 pl6-ns pl8 fa fa-ellipsis-h"></i>
        <%# <% end %>  
    <% end %>
<%# <%= list_item_close(params)  %>
</li>
    <%# first transaction will be on bottom of list, this will format appropriately %>
    <% if current_user.transactions.first == transaction %>
                <li class='list ph3'>
    <% else %>
                <li class='list ph3 bb b--light-silver'>
    <% end %>
    <div id="insert-show-more-<%=transaction.id%>"></div>
    

    
</li>
